# Supabase Setup (Updated)

Summary

Short instructions and references for setting up Supabase as the project's managed PostgreSQL + auth provider. For full environment details, see `README_GENERATED.md` and `Backend/config.py`.

Quick steps

1. Create a Supabase project
2. Copy `SUPABASE_URL` and `SUPABASE_KEY` into `Backend/.env`
3. Run DB migrations or use `Backend/scripts/create_tables.py`

Environment variables

- `SUPABASE_URL`
- `SUPABASE_KEY`

Status: Updated

Last updated: 2026-01-31
